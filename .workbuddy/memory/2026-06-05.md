# 2026-06-05

## 博客样式改造：移植 Next.js 模板排版到 Hugo

- 分析了 `nextjs-blog-template` (guangzhengli) 的完整设计系统，精确提取了 typography tokens
- 分析了 `hugo-theme-ladder` 主题，确认其已停止维护且不包含 LXGW WenKai 字体
- 决定保留现有 `hugo-theme-next` 主题，通过自定义 CSS 移植字体和排版

### 完成内容
- `static/css/custom_style.css` — 完整覆盖文章排版：
  - 字体：LXGW WenKai Lite (jsdelivr CDN)
  - 标题层级：h1 3.2rem → h6 0.8rem，font-weight 600
  - 段落：1.1rem / 行高 2rem / 上下间距 1rem
  - 行内代码：#ed4759 颜色 / #ededeb 背景 / 0.6rem 圆角
  - 引用块：3px #343a40 左竖线
  - 链接：下划线偏移 3px + 300ms 过渡动画
  - 表格、列表、分割线、图片全面覆盖
  - 深色模式适配（[data-theme="dark"]）
  - 移动端字号缩小
  - 标题锚点 # 符号悬停显示

### 技术细节
- Hugo 主题的 customFilePath.style 钩子正确加载自定义 CSS
- 使用 `!important` 确保覆盖主题原有的 SCSS 样式
- 在本地 http://localhost:1313 预览验证通过

---

## Hugo 博客主题切换：hugo-theme-next → hugo-theme-ladder

- 在 `theme-ladder` 分支上进行切换，保留 master 原主题不动
- 移除旧主题 submodule（hugo-theme-next），下载并安装 hugo-theme-ladder
- 重写 `config.yaml` 适配 Ladder 主题（YAML 格式，zh 语言，post/ 菜单）
- 补建缺失模板：`layouts/shortcodes/mermaid.html`（兼容原内容 Mermaid 图表）、`layouts/_default/rss.xml`（修复 Hugo v0.162+ RSS 渲染错误）
- 修复 3 个旧主题布局文件拦截问题：删除 `layouts/_default/section.html`、`layouts/index.html`、`layouts/partials/head/style.html`
- 配置头像：`static/imgs/liulu_avatar.png` → `/imgs/liulu_avatar.png`
- 清理旧主题残留：`layouts/partials/`、`static/css/` 目录
- 首页、/post/、/tags/、/archives/ 全部正常渲染
- 待处理：内容宽度偏窄（80rem），mqtt-introduction.md 的 Raw HTML 警告
